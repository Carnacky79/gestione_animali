<form method="post" action="{{ path('app_clienti_delete', {'id': clienti.id}) }}"
      onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ clienti.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
